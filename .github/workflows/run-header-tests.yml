name: fmi_3_ls_bus_header_test-Release

on:
  workflow_dispatch:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Install gtest manually
      run: sudo apt-get install libgtest-dev && cd /usr/src/gtest && sudo cmake CMakeLists.txt && sudo make && sudo ln -s /usr/lib/libgtest.a /usr/local/lib/libgtest.a && sudo ln -s /usr/lib/libgtest_main.a /usr/local/lib/libgtest_main.a
    - uses: actions/checkout@v1
    - name: configure
      run: mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Release ..
      working-directory: ./headers/tests
    - name: make
      run: cd build && make
      working-directory: ./headers/tests
    - name: Run Test
      run: /home/runner/work/fmi-ls-bus/fmi-ls-bus/build/test/fmi_3_ls_bus_header_test.test
